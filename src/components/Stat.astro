---
interface Props {
  value: string | number;
  label: string;
}

const { value, label } = Astro.props;
---

<div class="text-2xl font-bold">{value}</div>
<div class="text-gray-400 uppercase">{label}</div>
